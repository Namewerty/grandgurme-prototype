# Что не нужно загружать на хостинг при деплое.
#
# node_modules и dist уже исключены через .gitignore — Vercel ставит зависимости
# и собирает проект у себя. Здесь только то, что лежит в репозитории для работы,
# но к сборке отношения не имеет.

# Снимки экрана для обсуждения с заказчиком — 14 МБ, в сборку не входят.
screenshots

# Присланные заказчиком макеты. Не страницы прототипа: сборщик берёт только
# файлы index.html (см. findPages в vite.config.js).
Фильтры.html
grandgurme-homepage-mockup(2).html
