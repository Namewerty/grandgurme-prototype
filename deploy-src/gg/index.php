<?php
/**
 * Главная страница на новом шаблоне — пробный адрес /gg/.
 *
 * ПОЧЕМУ НЕ КОРНЕВОЙ index.php. Демо-магазин на eshop_bootstrap_v4 пока живёт
 * и не тронут: переключать сайт целиком нужно осознанно и одним действием,
 * а сравнивать удобнее, когда обе версии открыты рядом.
 *
 * Две константы объявляются ДО подключения шапки — она их читает:
 *   GG_HERO     — на странице есть первый экран: шапка над ним прозрачная,
 *                 логотип белый, логотип ведёт якорем к #hero;
 *   GG_SECTIONS — странице нужны секции главной (их собирает скрипт
 *                 из src/data/*.js прототипа — технический долг первого захода).
 */
define('GG_HERO', true);
define('GG_SECTIONS', true);

require($_SERVER['DOCUMENT_ROOT'] . '/bitrix/header.php');

$APPLICATION->SetTitle('№1 Гранд Гурмэ — чёрная и красная икра, рыба и морепродукты');
$APPLICATION->SetPageProperty('description', 'Бутик премиальной гастрономии: чёрная и красная икра, рыба, крабы и морепродукты, подарочные наборы. Доставка день в день по Москве и области.');

/* Содержимого здесь нет намеренно: десять секций главной собирает скрипт
   в #main. Когда тексты разложат по инфоблокам, секции переедут сюда
   компонентами, а data-gg-sections со страницы уйдёт. */

require($_SERVER['DOCUMENT_ROOT'] . '/bitrix/footer.php');
