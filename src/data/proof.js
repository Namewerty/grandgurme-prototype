/**
 * Доверие (#proof) — цифры, партнёры, документы.
 * Никаких карточек отзывов с цитатами и звёздочками: только проверяемое.
 *
 * ЦИФРЫ ЗАМЕНЕНЫ НА ДАННЫЕ ЗАКАЗЧИКА. Прежние четыре значения (180 / 14 /
 * 46 000 / 4,9 по 320 отзывам) были придуманы при сборке прототипа — их
 * больше нет. Сейчас здесь то, что заказчик назвал сам:
 *
 *   300           ресторанов и отелей — та же цифра, что в #brand и в hero;
 *   с 1982        год основания, он же в рамке страницы (см. README);
 *   100 000+      частных клиентов;
 *   4,9 / 1000    средняя оценка и число отзывов.
 *
 * ⚠ ЧТО ВСЁ ЕЩЁ НАДО ПОДТВЕРДИТЬ ПЕРЕД ЗАПУСКОМ: оценку 4,9 по 1000 отзывам
 * можно показывать только со ссылкой на публично проверяемый источник —
 * карточку Яндекс.Карт, отзовик или собственный модуль отзывов на сайте.
 * Если такого источника нет, цифру придётся убрать целиком: подтверждать
 * рекламные утверждения обязан рекламодатель. Остальные три — из внутренних
 * документов (реестр B2B-клиентов, дата регистрации юрлица, выгрузка CRM).
 *
 * Разряды разделены неразрывным пробелом прямо в значении: при отсчёте число
 * форматирует toLocaleString, но при prefers-reduced-motion отсчёта нет и на
 * экран попадает строка отсюда — «100000» без пробела читалось бы как опечатка.
 * Парсер разделители игнорирует, считать это не мешает.
 *
 * prefix / suffix — приставка и хвостик, которые НЕ участвуют в отсчёте:
 * «с » у года и «+» у числа клиентов. Без них строку пришлось бы совать
 * в само значение, и парсер получал бы NaN.
 *
 * count: false — значение не отсчитывается от нуля. Стоит у года основания:
 * бегущие «0 → 1982» читаются как сломанный счётчик, а не как приём.
 *
 * Значения отсчитываются при появлении секции — см. src/js/sections/proof.js.
 * Плейсхолдеров в тексте нет и быть не должно: прототип показывают заказчику.
 */

import { PRODUCTION_URL } from './production.js'

export const proofCopy = {
  eyebrow: 'НАМ ДОВЕРЯЮТ',
  title: 'Рестораны, отели и более 100 000 частных клиентов',

  documents:
    'Ветеринарные свидетельства, декларации соответствия и документы ' +
    'на происхождение — на каждую партию.',

  // Та же страница, что и у ссылки в #brand: одна точка правды по адресу.
  action: { label: 'Документы и производство', href: PRODUCTION_URL },
}

export const proofStats = [
  { value: '300', label: 'ресторанов и отелей по всему миру' },
  { value: '1982', prefix: 'с ', count: false, label: 'год основания' },
  { value: '100 000', suffix: '+', label: 'частных клиентов' },
  { value: '4,9', label: 'средняя оценка по 1000 отзывам' }, // ⚠ нужен публичный источник
]
