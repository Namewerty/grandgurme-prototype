/**
 * Контент шапки и верхней утилитарной полосы.
 *
 * Заказчик просил разгруженную шапку — референс по разгруженности
 * «Заморская лавка»: наверху минимум надписей, ассортимент раскрывается
 * внутри выпадающих панелей.
 *
 * ЖЁСТКОЕ ОГРАНИЧЕНИЕ: текстовых пунктов в шапке ровно четыре —
 * КАТАЛОГ, ИКРА, ПОДАРКИ, КОМПАНИЯ. Пятый добавлять нельзя: всё остальное
 * живёт внутри панелей и в подвале. Если раздел «просится» в шапку —
 * ему место в панели «Компания», в каталожной панели или в подвале.
 *
 * ЧТО ИЗМЕНИЛОСЬ ПО ПРАВКЕ ЗАКАЗЧИКА
 *
 * 1. «О бренде» → «КОМПАНИЯ». Пункт вёл на одну страницу /about и рядом
 *    с товарными читался как ещё одна страница о себе. Теперь это вход во все
 *    информационные страницы сайта — состав панели в src/data/company.js.
 *    Само название вынесено отдельным значением (navCompany.label): заказчик
 *    может захотеть «О нас» или «Информация», и это не должно требовать
 *    правок в разметке.
 * 2. В верхней полосе появились «Контакты»: три операционные ссылки слева
 *    (доставка, бутики, контакты) и связь справа. Это те вопросы, ответ
 *    на которые человек должен видеть без единого клика.
 * 3. Селектора региона в шапке нет и не будет — заказчик отказался явно.
 *    Города живут на странице бутиков и в блоке «Офлайн» на главной.
 */

import { ROUTES } from './routes.js'

export const contacts = {
  phone: '+7 925 466 66 46',
  phoneHref: 'tel:+79254666646',
  whatsapp: 'https://wa.me/79254666646',
  telegram: 'https://t.me/grandgurme',
  instagram: 'https://instagram.com/grandgurme',
  email: 'business@grandgurme.ru',
}

/**
 * Верхняя полоса. Уезжает вверх при прокрутке и не возвращается,
 * поэтому здесь только то, без чего можно жить дальше по странице.
 */
export const topbar = {
  start: [
    { label: 'Доставка и оплата', href: ROUTES.delivery },
    { label: 'Бутики', href: ROUTES.boutiques },
    { label: 'Контакты', href: ROUTES.contacts },
  ],
  end: [
    { label: contacts.phone, href: contacts.phoneHref },
    /* Мессенджеров у компании два, и выносить в полосу только один — значит
       выбирать за человеком. Пункт раскрывает маленький поповер с обоими.
       href настоящий и ведёт на /contacts: без JS и на мобильном, где полоса
       сжимается до телефона, пункт обязан куда-то приводить. Адреса берём
       из contacts, чтобы они не разошлись с подвалом и виджетом. */
    {
      label: 'Мессенджеры',
      href: ROUTES.contacts,
      menu: [
        { label: 'WhatsApp', href: contacts.whatsapp, external: true },
        { label: 'Telegram', href: contacts.telegram, external: true },
      ],
    },
  ],
}

/**
 * Главная точка входа в ассортимент. Оформлена капсулой с иконкой из трёх
 * линий и визуально отличается от остальных пунктов.
 *
 * href настоящий: с клавиатуры и на мобильном по нему уходят на /catalog,
 * на десктопе клик перехватывается и открывает панель (см. megamenu.js).
 */
export const navCatalog = {
  label: 'Каталог',
  href: ROUTES.catalog,
  panel: 'catalog',
}

/**
 * Левая группа после капсулы.
 *
 * ⚠ ПАНЕЛЕЙ У «ИКРЫ» И «ПОДАРКОВ» ПОКА НЕТ. Ключ panel здесь стоит как
 * заявка на будущее, но сборщика под него в src/js/nav/panels.js ещё не
 * написали — значит, это обычные ссылки, и шеврона у них НЕ БУДЕТ: шапка
 * рисует шеврон только там, где панель действительно поднимается
 * (см. src/js/sections/header.js). Появится сборщик — шеврон появится сам.
 *
 * «Икра» ведёт на чёрную икру: это флагман, и он же открыт в каталожной
 * панели по умолчанию. Когда у пункта появится своя панель, в ней будут
 * оба раздела — и чёрная, и красная.
 */
export const navPrimary = [
  { label: 'Икра', href: ROUTES.category('chernaya-ikra'), panel: 'caviar' },
  { label: 'Подарки', href: ROUTES.gifts, panel: 'gifts' },
]

/**
 * Информационный хаб справа — единственный текстовый пункт правой группы,
 * дальше идут иконки. Название отдельным значением: заказчик может
 * поменять его, не трогая разметку. Состав панели — src/data/company.js.
 */
export const navCompany = {
  label: 'Компания',
  href: ROUTES.about,
  panel: 'company',
}

export const navSecondary = [navCompany]

/**
 * Поиск. Не ссылка, а кнопка: по нажатию содержимое шапки гаснет и на его
 * месте разворачивается строка поиска (src/js/nav/search.js). Адрес страницы
 * результатов у него всё равно есть — на него уходят по Enter и с мобильного.
 *
 * ЗАКАЗЧИК ПРОСИЛ ВЫДЕЛИТЬ ПОИСК ЯВНО, ВОЗМОЖНО СЛОВОМ. Пятым текстовым
 * пунктом меню слово «Поиск» стоять не может — ограничение выше по файлу.
 * Но поиск и не раздел, а инструмент, и оформлен он как инструмент: капсула
 * на волосяной линии с лупой и приглушённой подписью, зеркальная капсуле
 * «Каталог» слева. Две капсулы держат шапку симметрично, между ними логотип.
 *
 * Три подписи — три ширины экрана, и все три лежат здесь, а не в разметке:
 *   field  ≥ 1280 — капсула с полным текстом;
 *   short  1024–1279 — капсула сжимается до лупы и одного слова;
 *   label  < 1024 — капсула становится иконкой, слово остаётся только
 *          для скринридера. Оно же озвучивается на всех ширинах.
 */
export const navSearch = {
  key: 'search',
  label: 'Поиск',
  field: 'Поиск по каталогу',
  short: 'Поиск',
  href: ROUTES.search,
  action: 'search',
}

/**
 * Иконочная группа справа. key — ключ из src/js/icons.js.
 *
 * Избранное и аккаунт — настоящие адреса, а не заглушки: сайт запускается
 * с регистрацией и личным кабинетом. Избранное живёт вкладкой внутри
 * кабинета, отдельного адреса у него нет.
 */
export const navActions = [
  navSearch,
  { key: 'heart', label: 'Избранное', href: `${ROUTES.account}?tab=favorites` },
  { key: 'user', label: 'Личный кабинет', href: ROUTES.account },
]

export const brandName = {
  num: '№1',
  name: 'Гранд Гурмэ',
  full: '№1 Гранд Гурмэ',
}

/** Мок состояния корзины — прототип, без бэкенда. */
export const cart = { count: 2, href: ROUTES.cart }
